export default function PlaceholderBanner() {
  return (
    <div className="bg-brand-100 text-brand-900 text-xs sm:text-sm text-center py-2 px-4">
      Preview build — address, phone number, opening hours, prices, team
      details and reviews are placeholders pending confirmation from the
      practice.
    </div>
  );
}
